{
  "name": "@law-analyzer/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "build": "tsc",
    "start": "bun --env-file=../../.env dist/index.js",
    "test": "bunx --bun vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@law-analyzer/ai": "workspace:*",
    "@law-analyzer/database": "workspace:*",
    "@law-analyzer/parser": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
